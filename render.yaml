services:
  - type: web
    name: garden-retakes-website
    env: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    disk:
      name: data
      mountPath: /data
      sizeGB: 10
    envVars:
      - key: PORT
        value: 10000
