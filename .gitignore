node_modules/
.next/
out/
.env
.env.local
.vercel
next-env.d.ts

# local experiments / downloaded asset packs (963MB+)
scratch/
public/pkmn/*.rar
.env.development.local

# Scratch build dir used to verify a build beside a running dev server
.next-verify/
.next-deploy/

# Local demo ingest — demos are large and never committed.
.demo-cache/

# Verification build output (NEXT_DIST_DIR), never committed.
.next-prod/

# Uploaded custom skin VPKs and their records — binaries, per-deployment state.
data/custom_skins/*
!data/custom_skins/.gitkeep
public/fastdl/*.vpk

# Uploaded feed clips and avatars — user content, not source.
data/clips/
data/avatars/

# Source2Viewer CLI — 50 MB of native binaries, fetched per-platform (tools/extract-radars.mjs)
tools/bin/
.aider*

# .NET build output for tools/map-screenshots
tools/map-screenshots/bin/
tools/map-screenshots/obj/
